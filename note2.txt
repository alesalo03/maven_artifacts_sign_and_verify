in prima battuta come viene fatta la compilazione con Maven in Java
maven build lifecycle --> una letta
Maven fornisce una standardizza una struttura ad albero dei sorgenti delle cartelle
Che sfrutta poi per fare delle azioni

Maven per estendere il suo lifecycle utilizza plugin esterni inglobabile nel lifecycle di base
plugin estende le funzionalità di base di una fase del lifecylce
pom.xml file che comanda tutto quello che fa Maven con due sezioni:
- dependencies ---> pacchetti esterni che deve portarsi a casa

Per la 1.3 se esistono artifact repository che fanno quella cosa
oltre ai due consigliati forse vanno meglio

nella 1.1 vedere se magari esiste un plugin Maven che come tecnologia CoSign 

Maven Central == repository centrale di Maven
